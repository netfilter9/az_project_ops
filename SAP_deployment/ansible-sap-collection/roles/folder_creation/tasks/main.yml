- name: main | folder creation
  file:
    path: "{{ item.path }}"
    state: directory
    owner: "{{ item.owner }}"
    group: "{{ item.group }}"
    mode: "{{ item.mode | default ('755') }}"
    attr: "{{ item.attr | default()}}"
  loop: "{{ folders }}"
