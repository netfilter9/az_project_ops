# Developer TODOs
* change to use a 'cluster.profile' input parameter to drive template selection
* use jinja2 includes to avoid duplication of common jinja2 code blocks - will make it easier to manage
* fix issues with hardcoded items such as netmasks
* improve structure for inputs
* update README