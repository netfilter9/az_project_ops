# Developer TODOs
* rename to "cluster-pcs"
* move ssh key copy to common role
* put configuration into a new role and allow profiles to be defined which handles differences between diff cloud providers and OSes
* add README.md