# Developer TODOs
* rename to sap-install
* add README.md
* simplify product references (no need for topology complexity)
* improve template approach - our current file is too large with too much duplication and there is probably little reason for most of the changes - do we really need all the ifs?
* find all the other roles where we set the same sysctl entries and remove them - we should only define here
