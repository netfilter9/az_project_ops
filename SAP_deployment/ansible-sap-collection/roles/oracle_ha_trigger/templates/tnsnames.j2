{{ sap.db.sid | upper }}.WORLD=
  (DESCRIPTION =
    (ADDRESS_LIST =
        (ADDRESS =
          (COMMUNITY = SAP.WORLD)
          (PROTOCOL = TCP)
          (HOST = {{ db_primary_hostname | default(dbnode1)  }})
          (PORT = {{ port | default('1521') }})
        )
        (ADDRESS =
          (COMMUNITY = SAP.WORLD)
          (PROTOCOL = TCP)
          (HOST = {{ db_secondary_hostname | default(dbnode2) }})
          (PORT = {{ port | default('1521') }})
        )
        (LOAD_BALANCE = yes)
    )
    (CONNECT_DATA =
       (SERVICE_NAME = {{ sap.db.sid | upper }})
    )
  )