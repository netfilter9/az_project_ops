# Developer TODOs
* rename role to 'domain-join'
* move the ntp component to its own role role
* create some worked examples in the cloudbuilder-examples repo
* update the README.md to mirror standards in crm_config (crm-config) role
* change input variables to more generic approach
