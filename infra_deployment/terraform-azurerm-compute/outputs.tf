# This module follows the "cloud builder" pattern so all outputs 
# are defined in the .tf file where the associated resource is 
# created.  This makes maintenance easier as it keeps asocaited
# components together.